Constraints check 
------------------

if n< 100             

---> O(2^N), N! ---> ex: bruteforce, backtracking

if n< 3000           

---> O(N^2)  ---> ex: Nested loops, dynamic programming

if 3000 < n < 10^6   

---> O(N), O(N Log N) ---> ex: two pointer, sliding window, binary serach, greedy, heap, sorting (merge, quick)

if n > 10^6          

---> O(Log N), O(1)   ---> ex: Binary search, math related 




Patterns 
----------


Cyclic sort --> if question --> array of integer contains range  of  {1, N} or {0,N} 
                 ex : find missing numbers, 
                 find duplicates, 
                 find all duplicats, 
                 find all numbers disappeared in the array, 
                 set mismatch
                 first missing postive 

Binary search --> if array is sorted  then try for it 
                ex : find target in sorted array

Modified Binary Search --> 
                ex : 